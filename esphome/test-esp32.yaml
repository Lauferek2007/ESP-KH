esphome:
  name: test-esp32
  friendly_name: Test ESP32

esp32:
  board: esp32dev
  framework:
    type: esp-idf

logger:

api:

ota:
  - platform: esphome

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  ap:
    ssid: "Test ESP32 Fallback"
    password: "fallback1234"

captive_portal:

web_server:
  port: 80

sensor:
  - platform: uptime
    name: "Test ESP32 Uptime"

switch:
  - platform: restart
    name: "Test ESP32 Restart"
